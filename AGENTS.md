# AGENTS.md

## プロジェクトの目的

このリポジトリは、CodexとGPU上で動作するLaya APIを組み合わせるための開発環境です。

汎用的なエージェント処理を前提とし、コア設計をカスタマーサポート、請求、部署、返金など特定の業務領域に依存させないでください。

## アーキテクチャ

- `codex`は、Codexがリポジトリを操作する開発コンテナです。
- `laya-api`はLayaモデルを読み込み、Composeネットワーク内のポート`8000`とホスト側のポート`17861`でAPIを提供します。
- Layaは、ルーティング、安全性、リトライ、エスカレーション、Tool選択、完了確認など、小さく限定された判断を担当します。
- Codexなどの大規模モデルは、広い推論、計画、コード生成、最終回答を担当します。
- 確定的なPolicy、権限確認、実行処理は通常のアプリケーションコードで管理します。確率的なLayaの判断だけを権限として扱わないでください。

## API設計ルール

- 判断APIは汎用的に保ち、入力または状態と、呼び出し側が指定するスキーマを受け取ります。
- コアAPIのリクエストやレスポンスに、特定業務専用のフィールドを追加しないでください。
- ハーネスが閾値やPolicyを適用できるよう、判断結果と利用可能な信頼度情報を返します。
- ルーティング、Policy確認、人間確認、リトライ、実行処理はモデル呼び出しの外側で管理します。
- 新しい判断を本番処理の制御に使う前に、Shadow Modeで検証します。
- 信頼度が低い場合やPolicyに違反する場合は、人間確認または安全な停止にします。

## 作業ルール

- アーキテクチャを変更する前に、既存ファイルを確認します。
- 作業ツリーにある無関係なユーザー変更を保持します。
- `OPENAI_API_KEY`などの秘密情報を追跡対象ファイルへ保存しません。
- Compose、Dockerfile、モデル依存関係を変更した場合は、適切な設定検証または構文チェックを実行します。
- 特定業務向けのデモだけを唯一の利用方法にしません。例を置く場合も、基盤APIは再利用できるようにします。

## 開発用コマンド

サービスを起動するには次を実行します。

```sh
docker compose up --build
```

主なエンドポイントは次の通りです。

- Laya UI: `http://localhost:17860/ui`
- Laya API: `http://localhost:17861`
- APIドキュメント: `http://localhost:17861/docs`
- 起動状態確認: `http://localhost:17861/readyz`

Layaモデルは`laya_models`ボリュームに保存されます。`codex`のワークスペースマウントは開発コンテナ内からリポジトリを編集するためのもので、モデル用ボリュームとは別です。
